name: Run DB Migrations (one-time)

on:
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install dependencies
        run: npm ci
      - name: Resolve failed migration
        run: npx prisma migrate resolve --applied 20260304103000_owner_chat_intent_payload
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          URBANEX_DATABASE_URL: ${{ secrets.URBANEX_DATABASE_URL }}
      - name: Run all pending migrations
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          URBANEX_DATABASE_URL: ${{ secrets.URBANEX_DATABASE_URL }}
